export const website_domain = 'app.deriv.com';

/**
 * Your site’s public name (landing logo). Also update `index.html` and `public/manifest.json` for tab title & PWA.
 */
export const site_display_name = 'TradeNest';

/**
 * OAuth `brand` parameter — keep `Deriv` unless Deriv’s API docs say otherwise for your app.
 */
export const website_name = 'Deriv';

export const default_title = site_display_name;

/** Short line for meta / sharing; optional polish for SEO. */
export const site_tagline = 'Automated trading with Deriv bots — simple, visual, your account.';

export const TRACKING_STATUS_KEY = 'tracking_status';
